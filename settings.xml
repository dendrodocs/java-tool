<settings>
    <servers>
        <server>
            <id>central</id>
            <username>${env.MAVEN_USER}</username>
            <password>${env.MAVEN_PASSWORD}</password>
        </server>
    </servers>
</settings>